using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyInput : State {
    /*
	// Use this for initialization
	void Awake () {
        setup("Default");
	}
    

    // Update is called once per frame
    void FixedUpdate () {
        C_movement.push(new Vector2(Random.Range(-1, 2), Random.Range(-1, 2)).normalized * 8);
        
	}
    */

}
